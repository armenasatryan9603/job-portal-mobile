/**
 * Firebase Configuration - FCM Only
 * 
 * This file is kept for backward compatibility but is minimal.
 * Firebase Messaging (@react-native-firebase/messaging) auto-initializes
 * when used in NotificationService.
 * 
 * We only use Firebase Cloud Messaging (FCM) for push notifications.
 * Other Firebase services are disabled to reduce costs.
 */

// Firebase Messaging will auto-initialize when NotificationService uses it
// No explicit initialization needed here

export default null;
